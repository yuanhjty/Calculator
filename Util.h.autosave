#ifndef CONSTANTS_H
#define CONSTANTS_H

#include <vector>
#include <map>
#include <string>

extern const std::vector<std::string> operators;
extern const std::map<std::string, int> operatorPriority;
extern const std::string digits;
extern enum TOKEN {

extern isNumber(const std::string& token);
extern isOperator(const std::string& token);

#endif // CONSTANTS_H